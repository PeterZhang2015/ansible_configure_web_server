---
# Local environment SSH configuration
# Uses local connection, no SSH needed
ansible_connection: local
ansible_python_interpreter: /usr/bin/python3
# For local development, prompt for sudo password
ansible_become_method: sudo
